import { HoverJobCardDirective } from './hover-job-card.directive';

describe('HoverJobCardDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverJobCardDirective();
    expect(directive).toBeTruthy();
  });
});
